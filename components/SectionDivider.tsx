interface SectionDividerProps {
  topColor?: string
  bottomColor?: string
  variant?: "curve" | "wave" | "slant"
  goldLine?: boolean
}

// Sections use alternating background colours — no decorative dividers needed.
export default function SectionDivider(_props: SectionDividerProps) {
  return null
}
